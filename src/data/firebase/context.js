import React from 'react';
// Declara el contexto de firebase, que permite que firebase exista en toda la aplicación
const FirebaseContext = React.createContext(null);

export default FirebaseContext;
