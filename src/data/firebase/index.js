import FirebaseContext from './context';
import Firebase from './firebase';
// Sirve para agrupar todas las funciones de firebase en un solo lugar
export default Firebase;

export { FirebaseContext };